# Dialog

```tsx
import { Dialog, DialogTrigger, DialogContent } from 'ui'
```

```tsx live=true
<Button>Connect Wallet</Button>
```

## Props

<PropsTable componentSourceCodeLink={componentSourceCodeLink} types={types} />


## Icon variant

```tsx live=true
<div className="flex items-center gap-2">  
  <Button variant="icon" size="sm">
    <CloseIcon title="wallet" />
  </Button>

  <Button variant="icon">
    <WalletIcon title="wallet" />
  </Button>
  
  <Button variant="icon">
    <MenuIcon title="wallet" />
  </Button>
</div>
```