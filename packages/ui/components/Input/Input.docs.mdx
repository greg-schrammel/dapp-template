# Input


```tsx
import { Input } from 'ui'
```

```tsx live=true
<Input placeholder="Search address" left={<SearchIcon className="text-low -ml-1 text-xl" />} />
```

## NumberInput
- Only numbers (no `e`s)
- Auto formats based on region 
- Number keyboard on mobile

_Note: will probably be merged into `<Input />` in the future_  

```tsx live=true
<NumberInput placeholder="0.00"  />
```

## Props

<PropsTable sourceLink={sourceLink} types={types} />
